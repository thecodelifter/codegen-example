import styled from 'styled-components';

export const Form = styled.form`
  display: flex;
  max-width: 300px;
  width: 100%;
  flex-direction: column;
  margin: 0 auto;

  > label,
  button {
    margin-top: 0.5rem;
  }
`;
